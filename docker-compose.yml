x-env-file: &compose_env_file ${COMPOSE_ENV_FILE:-.env}

services:
  postgres:
    image: postgres:15
    container_name: meme-wrangler-db
    restart: unless-stopped
    profiles: ["", "local"]  # excluded in the "neon" profile
    env_file:
      - *compose_env_file
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-meme_wrangler}
      - POSTGRES_USER=${POSTGRES_USER:-meme}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-meme}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-meme} -d ${POSTGRES_DB:-meme_wrangler}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  meme-wrangler:
    build: .
    container_name: meme-wrangler
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
        required: false  # allow running without local PG when using Neon
    env_file:
      - *compose_env_file
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
      - OWNER_ID=${OWNER_ID:-}
      - CHANNEL_ID=${CHANNEL_ID:-}
      - POSTGRES_DB=${POSTGRES_DB:-meme_wrangler}
      - POSTGRES_USER=${POSTGRES_USER:-meme}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-meme}
      - POSTGRES_HOST=${POSTGRES_HOST:-postgres}
      - POSTGRES_PORT=${POSTGRES_PORT:-5432}
      - DATABASE_URL=${DATABASE_URL:-postgresql://${POSTGRES_USER:-meme}:${POSTGRES_PASSWORD:-meme}@${POSTGRES_HOST:-postgres}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-meme_wrangler}}
      - MEMEBOT_BACKUP_DIR=${MEMEBOT_BACKUP_DIR:-/app/backups}
      - MEMEBOT_BACKUP_INTERVAL_HOURS=${MEMEBOT_BACKUP_INTERVAL_HOURS:-6}
      - MEMEBOT_BACKUP_RETAIN_COUNT=${MEMEBOT_BACKUP_RETAIN_COUNT:-10}
      - MEMEBOT_BACKUP_STORE_IN_DB=${MEMEBOT_BACKUP_STORE_IN_DB:-true}
    volumes:
      - ./backups:/app/backups
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  pgdata:
